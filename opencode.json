{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "deepseek": {
      "name": "DeepSeek"
    },
    "minimax-coding-plan": {
      "name": "MiniMax (coding plan)"
    }
  },
  "mcp": {
    "github-mcp": {
      "type": "remote",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer {env:GH_PAT}"
      }
    },
    "azure-mcp": {
      "type": "remote",
      "url": "http://localhost:52117/mcp",
      "enabled": false,
      "__comment": "Blocked on Alpine/musl (glibc binary). Unblocks when k3s migration (#44) lands — azmcp runs as a glibc-based sidecar container in the same pod, reachable at localhost. Config is pre-switched to 'type: remote' + localhost URL for the k3s target state."
    }
  }
}