http:// {
    respond "Caddy works."
}

# DNS (A record for cloudlab.cloud5.ovh) and Let's Encrypt TLS — deferred to #23, #24
# Replace the respond line above with reverse_proxy routes as services are deployed.