---
- name: Configure cloudlab VPS
  hosts: cloudlab
  become: true
  roles:
    - common
    - security
    - azure_arc
    - docker_host
    - docker_services
    - docker_opencode_ingress
    - docker_opencode_instances
